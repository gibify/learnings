import { createSlice, PayloadAction } from '@reduxjs/toolkit';

export const todoSlice = createSlice({
  name: 'name',
  initialState: [],
  reducers: {},
});

// export const { todoCreate } = todoSlice.actions